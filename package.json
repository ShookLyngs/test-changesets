{
  "name": "test-changesets-action",
  "version": "0.1.1",
  "scripts": {
    "release:packages": "changeset publish",
    "release:snapshot": "git reset --hard HEAD && npx changeset version --snapshot snap"
  },
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@changesets/get-github-info": "^0.6.0",
    "@changesets/types": "^6.0.0",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.5.0"
  }
}
